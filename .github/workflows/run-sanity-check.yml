name: Build and Test

on:
  push:
    branches: [ main, develop, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    
    - name: Run Maven clean test
      run: mvn clean test
